# Regenerable data / figures -- not committed.
#   mbhb_simbank.npz -> python make_tutorial_simbank.py (needs the
#   dsbi-ldc-mbhb repo + LISA waveform stack; see README)
*.npz
*.png
__pycache__/

# Slide-deck assets are not regenerable figures -- they travel with the deck
# that GitHub Pages serves out of docs/.
!docs/img/*.png

# unrelated local repo living inside this folder
260617 - ICTP SAIFR Colloquium/
